{
    "rules": {
        ".read": true,
        "stats": {
            "total_installs": {
                ".validate": "newData.isNumber() && (!data.exists() || newData.val() == data.val() + 1)",
                ".write": true
            },
            "total_warnings": {
                ".validate": "newData.isNumber() && (!data.exists() || newData.val() == data.val() + 1)",
                ".write": true
            },
            "total_queries": {
                ".validate": "newData.isNumber() && (!data.exists() || newData.val() == data.val() + 1)",
                ".write": true
            }
        }
    }
}